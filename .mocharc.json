{
  "watch-extensions": "ts",
  "recursive": true,
  "reporter": "spec",
  "timeout": 600000,
  "require": ["ts-node/register"],
  "experimental-modules": true,
  "extensions": [".ts"],
  "spec": ["test/**/*.test.ts"],
  "node-option": [
    "experimental-vm-modules",
    "loader=ts-node/esm"
  ]
}
